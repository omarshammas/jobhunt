%h1= "#{@funding_rounds.size} Funding Rounds"
= render partial: 'table', locals: { funding_rounds: @funding_rounds }

%br
= link_to 'New Funding Round', new_funding_round_path, class: 'btn btn-primary'
