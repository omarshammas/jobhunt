%p#notice= notice

%p
  %b Name:
  = @investor.name

%br
%h3 Funding Rounds
= render partial: 'funding_rounds/table', locals: { funding_rounds: @investor.funding_rounds.order(:announced_on) }

= link_to 'Edit', edit_investor_path(@investor), class: 'btn btn-default'
= link_to 'Back', investors_path, class: 'btn btn-default'
